spring.application.name=FantaParco

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/FantaParco
spring.datasource.username=postgres
spring.datasource.password=${PASSWORD_POSTGRES}  # Modificato
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.hibernate.ddl-auto=update

# Stripe Configuration
stripe.secret.key=${STRIPE_SECRET_KEY}           # Modificato
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}    # Modificato

# Gmail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_MAIL_FROM}            # Modificato
spring.mail.password=${GMAIL_MAIL_FROM_PASSWORD}    # Modificato
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

# Path to Logo
path.to.logo=logo.png
